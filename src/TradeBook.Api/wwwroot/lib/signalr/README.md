# @microsoft/signalr 9.0.19 (browser build)

`signalr.min.js` is `dist/browser/signalr.min.js` from the npm package
`@microsoft/signalr` version 9.0.19, MIT licence, copied here unchanged so the
page works without internet access. To upgrade, replace the file and update
this note.
